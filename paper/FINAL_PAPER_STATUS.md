# Final Paper Status

The v1 manuscript is superseded by the corrected TireTwin v2 model.

A scientifically honest final paper cannot be generated until `runResearchAnalysisV2.m` is executed on the regenerated v2 telemetry files. The old baseline, sensitivity, and Monte Carlo numbers must not be copied into the v2 manuscript because the model equations and uncertainty design have materially changed.

After the v2 run, use the generated `results/v2_*.csv`, figures, telemetry manifest, and run manifest to produce the final paper.
